# Vite client-side environment variables

# Optional explicit API URL (defaults to /api in production, localhost:8080/api in dev via config)
VITE_API_URL=http://localhost:8080/api

# Optional request timeout for API calls made by the dashboard frontend (milliseconds)
VITE_API_TIMEOUT_MS=30000
